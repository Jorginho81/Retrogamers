{% extends 'base.html.twig' %}

{% block title %}Attributs{% endblock %}

{% block body %}
    <h1>Attributs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ matiere.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ matiere.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_matiere_index') }}">Retour</a>

    <a href="{{ path('app_matiere_edit', {'id': matiere.id}) }}">Modifier</a>

    {{ include('matiere/_delete_form.html.twig') }}
    
{% endblock %}
