<form method="post" action="{{ path('app_pages_admin_delete', {'id': pages_admin.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cet item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pages_admin.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
